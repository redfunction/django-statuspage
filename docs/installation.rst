============
Installation
============

At the command line::

    $ easy_install django-statuspage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-statuspage
    $ pip install django-statuspage
